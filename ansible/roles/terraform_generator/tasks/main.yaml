---
# Main tasks for Terraform generation and execution

- name: "Include workspace preparation tasks"
  include_tasks: prepare_workspace.yaml
  tags:
    - terraform
    - workspace

- name: "Include template generation tasks"
  include_tasks: generate_templates.yaml
  tags:
    - terraform
    - generate

- name: "Include Terraform execution tasks"
  include_tasks: execute_terraform.yaml
  tags:
    - terraform
    - execute

- name: "Include output processing tasks"
  include_tasks: process_outputs.yaml
  tags:
    - terraform
    - outputs