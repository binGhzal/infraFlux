---
# Image Reflector Controller for scanning container registries
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: image-reflector-controller
  namespace: flux-system
spec:
  interval: 30m
  chart:
    spec:
      chart: image-reflector-controller
      version: ">=0.31.0"
      sourceRef:
        kind: HelmRepository
        name: fluxcd
        namespace: flux-system
      interval: 12h
  values:
    # Controller configuration
    replicaCount: 1
    
    image:
      repository: fluxcd/image-reflector-controller
      tag: "v0.31.2"
      
    resources:
      limits:
        cpu: 1000m
        memory: 1Gi
      requests:
        cpu: 100m
        memory: 64Mi
        
    # Security context
    securityContext:
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      runAsNonRoot: true
      capabilities:
        drop: ["ALL"]
        
    # Monitoring
    prometheus:
      enabled: true
      
    # Registry scanning configuration
    env:
      - name: RUNTIME_NAMESPACE
        valueFrom:
          fieldRef:
            fieldPath: metadata.namespace