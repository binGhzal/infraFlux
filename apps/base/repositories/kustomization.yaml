apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: helm-repositories
  annotations:
    config.kubernetes.io/local-config: "true"

resources:
  - common-repositories.yaml
  
# HelmRepository definitions for commonly used Helm charts
# These provide sources for HelmReleases in the applications